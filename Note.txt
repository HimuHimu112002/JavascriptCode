1 = Map and ForEach er majhe parthokko
Ans = Map array return kore and ForEach array return korena

2 = ForOf and Forin Loop er majhe parthokko
Ans = ForOf use hoi array er jonno and Forin use hoi object er jonno

3 = Class 
class student{
    constructor(name){
        this.name = name // akhane this.name use na kore just name use korle object faka asbe
    }
}
const re = new student("Aktarujjaman")
console.log(re)

4 = null typeof -> object

5 = console.log(Math.max()) // result -infinity

6 = var a = Math.max() < Math.min(); // true
    var b = Math.max() > Math.min(); // false
    // akhane dui condition min holo max er theke boro

7 = console.log(true + true + true * 3) // 5 true mane 1

8 = console.log(NaN === NaN) // false
    console.log(String === String) // true
    console.log(null === null) // true
    console.log(Boolean === undefined) // true
    console.log(Number === Number) // true